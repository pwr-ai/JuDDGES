defaults:
  - model: ???
  - dataset: pl-court-instruct
  - _self_

device_map: 'auto'
output_file: data/experiments/predict/${dataset.name}/outputs_${model.name}.json
metrics_file: data/experiments/predict/${dataset.name}/metrics_${model.name}.json

max_new_tokens: 250
truncate_context: True

hydra:  
  output_subdir: null  
  run:  
    dir: .
