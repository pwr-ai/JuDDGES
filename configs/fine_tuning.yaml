defaults:
  - model: Mistral-7B-Instruct-v0.2
  - dataset: pl-court-instruct
  - _self_

output_dir: data/experiments/fine-tune/${model.name}/${dataset.name}
run_name: ${model.name}_${dataset.name}_fine_tune

wandb_entity: graph-ml-lab-wust
wandb_project: juddges-fine-tune

truncate_context: True

hydra:  
  output_subdir: null  
  run:  
    dir: .
